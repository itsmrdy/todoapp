name: application
description: A module that orchestrates what can be done within the application by defining use cases.

publish_to: none

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  bloc: ^7.1.0
  core:
    path: ../../core

  domain:
    path: ../domain

  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  module:
    androidX: true
    androidPackage: com.gcvh.application
    iosBundleIdentifier: com.gcvh.application
